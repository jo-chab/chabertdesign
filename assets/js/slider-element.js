	/*ELEMENTS*/	const carouselSlide = document.querySelector('.carousel-slide');	const carouselCards = document.querySelectorAll('.carousel-slide .project-card');	/*const content1 = document.querySelector('.slide-content:nth-child(1)');	const content2 = document.querySelector('.slide-content:nth-child(2)');	const textToLoad2 = content2.childNodes;	const data1 = content1.dataset.test;	const data2 = content2.dataset.test;	console.log(data1);	console.log(data2);*/		const nextBtn = document.querySelector('#nextBtn');		const size = carouselSlide.clientWidth;	let counter = 0;		nextBtn.addEventListener('click', () => {		if (counter >= carouselCards.length -1) return;		carouselSlide.style.transition = "transform 0.4s ease-in-out";		counter++;		carouselSlide.style.transform = 'translateX(' + (-size * counter) + 'px)';		/*content1.style.opacity = "0";		content2.style.opacity = "1";		content2.style.visibility = "inherit";		textToLoad2.style.transform = 'matrix(1, 0, 0, 1, 0, 0)';*/	});		carouselSlide.addEventListener('transitionend', () => {		if (carouselCards[counter].id === 'firstClone') {			carouselSlide.style.transition = "none";			counter = 0;			carouselSlide.style.transform = 'translateX(' + (-size * counter) + 'px)';		}	});