<?php    $element = $_GET['project'];?><div class="dashboard-main">    <div class="dashboard-title">Images du projet</div>        <div class="dashboard-content">                <form method="post" action="action_project_img-add" enctype="multipart/form-data">                        <input type='hidden' name='element' value='<?php echo $element ?>'>                        <ul class="list-img-project">                <?php                    $stmt2 = $db->prepare('SELECT image_id FROM project_images WHERE project_id = ?');                    $stmt2->execute([$element]);                    $selectedImgs = $stmt2->fetchAll(PDO::FETCH_COLUMN, 0); // Get an array of selected types                                        $sql1 = $db->prepare('SELECT * FROM images ORDER BY id_image ASC');                    $sql1->execute();                                        while ($row = $sql1->fetch(PDO::FETCH_ASSOC)) {                        $id_image = $row['id_image'];                        $img = urldecode($row['url']);                        $isChecked = in_array($id_image, $selectedImgs) ? 'checked' : '';                        echo "                        <label class='custom-checkbox'>                    <input type='checkbox' name='selected_imgs[]' value='$id_image' $isChecked>                    <img src='$img' width='' height='' />                </label>                        ";                    }                ?>            </ul>                        <div class="d-form-cta">                <div class="btn-primary btn-confirm small icon mobile-icon-only"><span>Valider la selection</span></div>            </div>        </form>        </div></div>